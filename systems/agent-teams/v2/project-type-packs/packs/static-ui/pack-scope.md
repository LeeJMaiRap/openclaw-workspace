# Pack Scope — Static UI Pack

## Use When

```text
static HTML/CSS/JS, mock/local data UI
```

## Do Not Use When

```text
project needs production/cloud/secrets/deploy by default
primary shape belongs to another pack
claim needs browser/security/performance evidence not available
```
