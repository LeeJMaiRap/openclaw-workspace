# Stop Gates — <Pack Name>

Stop and ask before:

```text
install/download
package manager switch
browser binary install
cloud/DNS/billing/secrets/deploy
production claim
security/performance claim without specialist evidence
```
