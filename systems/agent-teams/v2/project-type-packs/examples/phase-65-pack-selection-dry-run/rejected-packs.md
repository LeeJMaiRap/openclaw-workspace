# Rejected Packs

## HTTP API Pack

Rejected because primary interface is not HTTP endpoints.

## Static UI Pack

Rejected because no static UI is requested.

## Fullstack Local Pack

Rejected because there is no UI + API split.

## Docs-Only / Project-Management Pack

Rejected because desired output is working CLI, not only docs/process artifacts.
