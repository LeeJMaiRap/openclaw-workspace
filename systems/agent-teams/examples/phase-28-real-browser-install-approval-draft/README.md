# Phase 28 — Real Browser Install Approval Request Draft

## Purpose

Draft a concrete approval request for installing the Playwright Chromium browser binary.

## Scope

Paper documentation only.

No install, no download, no dependency change, no app code change.

## Exact Command Drafted

```sh
npx playwright install chromium
```

## Why This Exists

Phase 22 showed Playwright package availability but missing Chromium executable. Real browser proof and screenshot proof remain Blocked until a browser executable can launch.

Phase 28 prepares the approval request, but does not execute it.
